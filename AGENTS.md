# LinearPlus Agent Notes

Start from the live CLI:

```bash
bin/linearplus --help
bin/linearplus greenmark-bootstrap --dry-run
python verify.py
```

Do not use browser UI automation as the primary path for initiative creation.
Use the official Linear GraphQL API through the CLI and preserve redacted
evidence for Eidos loops.
